.sixteen.wide.column 
  .row
    br
    if messages.errors
      .ui.negative.message
        i.close.icon
        for error in messages.errors
          div= error.msg
    if messages.info
      .ui.warning.message
        i.close.icon
        for info in messages.info
          div= info.msg
    if messages.success
      .ui.positive.message
        i.close.icon
        for success in messages.success
          div= success.msg
    if messages.final
      .ui.icon.positive.message
        i.trophy.icon(style='color:gold')
        .content
          .header
            | Thank you for participating in our study
          ul.list
            li The study is now over and your account has been disabled.
            li
              | Please complete the survey 
              a(href=messages.final[0].msg, style='color:red;font-size: 19px') here, 
              | to ensure that you will be paid for your participation in the study.
