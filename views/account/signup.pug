extends ../ui_layout

block content
  .computer.only.tablet.only.eight.wide.column
    img.ui.fluid.image(src='/public/homepage.jpg')
  
  .eight.wide.computer.eight.wide.tablet.sixteen.wide.mobile.column
    h2.yi.header(style="text-align:center;") Welcome to 
    h1.yi.header(style="text-align:center;color:green") Make it Happen!
      .ui.horizontal.divider
        | Sign Up
    form.ui.form(id='signup-form', method='POST')
      input(type='hidden', name='_csrf', value=_csrf)
      .field
        label(for='username',data-title="User Name") User Name
        input(type='text', name='username', id='username', placeholder=' Choose a Username', autofocus, required,title="Please don't use your real (full) name.")

      .field
        label(for='prolificID') Prolific ID
        input(type='text', name='prolificID', id='prolificID', placeholder=' Enter your Prolific ID', required,title="Please fill in your Prolific ID.",Value='')    
      //.field
        //.ui.checkbox
          //input.hidden(type='checkbox', name='tos', id='tos', required)
          //label
            //| I agree to the 
            //a(href='/tos') Terms and Conditions
      //button.ui.button(type='submit') Signup
      .ui.horizontal.divider
        | and
      button.ui.big.green.labeled.icon.button.signup(type="submit",id='signupBTN')
        i.signup.icon
        |   Submit

